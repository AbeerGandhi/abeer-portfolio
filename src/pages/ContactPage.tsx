import Contact from "../components/Contact";

const ContactPage = () => {
    return (
        <div className="page-wrapper container1" style={{ paddingTop: '120px', height: 'auto', minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
            <Contact />
        </div>
    );
};

export default ContactPage;
